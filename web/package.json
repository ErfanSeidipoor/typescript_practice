{
  "devDependencies": {
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "axios": "^0.19.1"
  },
  "scripts": {
    "start:db": "json-server -w db.json --no-cors",
    "start:parcel": "parcel index.html"
  }
}
